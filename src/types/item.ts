export interface Item {
  id: string;

  description: string;

  unit: string;

  showSize: boolean;
  showQty: boolean;
  showRate: boolean;

  size: string;
  qty: string;
  rate: string;

  total: string;

  remarks: string;
}